class Solution:
    def arraySign(self, nums: List[int]) -> int:
        p=nums[0]
        for i in range(1,len(nums)):
            p*=nums[i]
        if p==0: return 0
        elif p>0: return 1
        else: return -1
